# Cloud Run deployment configuration for bot-service staging environment
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: bot-service-staging
  labels:
    cloud.googleapis.com/location: us-central1
    service: bot-service
    environment: staging
  annotations:
    run.googleapis.com/ingress: all
    run.googleapis.com/execution-environment: gen2
    run.googleapis.com/description: "Bot Service - Staging Environment"
spec:
  template:
    metadata:
      labels:
        service: bot-service
        environment: staging
        version: "latest"
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "5"
        run.googleapis.com/cpu-throttling: "true"
        run.googleapis.com/execution-environment: gen2
        run.googleapis.com/vpc-access-connector: projects/PROJECT_ID/locations/us-central1/connectors/bot-service-connector
        run.googleapis.com/vpc-access-egress: private-ranges-only
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      serviceAccountName: bot-service@PROJECT_ID.iam.gserviceaccount.com
      containers:
      - image: us-central1-docker.pkg.dev/PROJECT_ID/bot-service-repo/bot-service:latest
        name: bot-service
        ports:
        - name: http1
          containerPort: 8080
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "staging"
        - name: PORT
          value: "8080"
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_HOST
          value: "10.0.0.10"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "bot_service_staging"
        - name: DB_USER
          value: "bot_service_user"
        - name: DB_SSL_MODE
          value: "require"
        - name: REDIS_HOST
          value: "10.0.0.20"
        - name: REDIS_PORT
          value: "6379"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              key: latest
              name: openai-api-key
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              key: latest
              name: database-password-staging
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: latest
              name: redis-password-staging
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
          requests:
            cpu: "0.5"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
            httpHeaders:
            - name: User-Agent
              value: "GoogleHC/1.0"
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/ready
            port: 8080
            httpHeaders:
            - name: User-Agent
              value: "GoogleHC/1.0"
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
  traffic:
  - percent: 100
    latestRevision: true